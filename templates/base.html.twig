<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="https://bootswatch.com/4/spacelab/bootstrap.min.css">
    {% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}">3WA Blog</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}">Accueil</a>
            </li>
        </ul>
        <ul class="navbar-nav">
        {% if not is_granted("ROLE_USER") %}
        
            <li class="nav-item">
                <a class="btn btn-info" href="{{ path('account_register') }}">Inscription</a>
            </li>
            <li class="nav-item">
                <a class="btn btn-success" href="{{ path('security_login') }}">Connexion</a>
            </li>
        {% else %}
            <li class="nav-item text-white">
                Connecté en tant que {{ app.user.firstName }}
            </li>
            <li class="nav-item">
                <a class="btn btn-warning" href="{{ path('security_logout') }}">Déconnexion</a>
            </li>
        {% endif %}
            
        </ul>

        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Search">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
        </form>
    </div>
</nav>

<div class="container pt-5">
    {% block body %}{% endblock %}
</div>
{% block javascripts %}{% endblock %}
</body>
</html>
